# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# then run: python -m pytest tests/ -q
-r requirements.txt
pytest>=7.4.0
pytest-asyncio>=0.23.0
